import styled from 'styled-components';

const CloseMenu = styled.section`
    position: fixed;
    height: 100%;
    width: 100%;
    left: 0;
    top: 70px;
    background-color: rgba(0, 0, 0, .7);
    z-index: 2;
`;

export default CloseMenu;